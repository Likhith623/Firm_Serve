//import middleware from "next-auth/middleware";
//or
export { default } from "next-auth/middleware";
export const config = {
    matcher: [
        // '/clients/:path*',
        // '/projects/:path*',
        // '/dashboard/:path*',
        // '/settings/:path*',
        // '/users/:path*',
        // '/api/auth/signin'
    ],
};